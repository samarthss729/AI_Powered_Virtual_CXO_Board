# Demo mode — run the full board without OpenAI (no API key required)
DEMO_MODE=true
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Real OpenAI mode — set DEMO_MODE=false and provide a valid key
# DEMO_MODE=false
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
